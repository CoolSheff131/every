mixin blockSection(title,subtitle,bgn)
    .first(style=`background-image: url(${bgn});`)
        .first__left
            h1= title
            h5= subtitle
            .first__email
                input(type="email",placeholder="Введите email" )
                button Узнать